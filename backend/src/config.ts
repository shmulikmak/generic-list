export const config = {
  BASE_URL: process.env.API_BASE_URL || 'https://jsonplaceholder.typicode.com/',
  ENDPOINTS: {
    users: 'users',
    photos: 'photos',
    // Add more endpoints as needed
  },
};
