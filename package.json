{
  "name": "generic-list-task",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && npm run start\" \"cd frontend && ng serve\"",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && ng serve\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "clean": "rm -r node_modules backend/node_modules frontend/node_modules"

  },
  "devDependencies": {
    "concurrently": "~8.2.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
